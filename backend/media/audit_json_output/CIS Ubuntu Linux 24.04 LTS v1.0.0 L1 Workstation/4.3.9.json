{
  "check_type": "CONDITIONAL",
  "condition": {
    "type": "AND",
    "rules": [
      {
        "type": "CMD_EXEC",
        "description": "nftables active",
        "cmd": "/bin/systemctl is-active nftables",
        "expect": "^active$"
      }
    ]
  }
}